// const currentUserHandler = require("../../requests/currentUser");
// const fetch = require("node-fetch");

test.todo("fix these tests");
// jest.mock("fetch");

// test("should pass null if no user", () => {
//   const req = { session: { id: 1, username: "Isabel" } };
//   const res = {
//     username: currentlyLoggedinUser.username,
//     id: currentlyLoggedinUser.id,
//   };
//   fetch.get.mockResolvedValue(res);
//   return currentUserHandler.all().then((data) => expect(data).toEqual(req));
// });

// test("should return user id and name if there is a user", () => {
//   //   const req = { session: { id: 1, username: "Isabel" } };
//   //   const res = {};
//   //   expect(currentUserHandler(req, res)).toBe({
//   //     username: currentlyLoggedinUser.username,
//   //     id: currentlyLoggedinUser.id,
//   //   });
// });
